import { renderMain, renderGallow } from "./utils/generateField.js";
import { renderKeyboard, renderWrapperKeyboard } from "./utils/renderKeyboard.js";

renderMain();
renderGallow();

renderWrapperKeyboard();
renderKeyboard();